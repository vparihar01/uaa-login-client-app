<h1>Listing clients</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Scope</th>
    <th>Authorized Grant Types</th>
    <th></th>
    <th></th>
  </tr>

<% @clients.each do |client| %>
  <tr>
    <td><%= client["client_id"] %></td>
    <td><%= client["scope"] %></td>
    <td><%= client["authorized_grant_types"]%></td>

  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Client', new_client_path %>
